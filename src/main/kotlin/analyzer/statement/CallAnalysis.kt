package analyzer.statement

import analyzer.AnalysisContext
import analyzer.nextContext
import python.PythonType

object CallAnalysis {

    fun analyze(call: PythonType.Expression.Call, context: AnalysisContext) = nextContext(context) {
        if (call.)
    }

}